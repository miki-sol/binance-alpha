# BSC Token Tracker Bot

NestJS приложение для отслеживания прихода ERC20 токенов на кошельки в сети BSC с автоматическим открытием шорт позиций на Gate.io.

## Возможности

- 📊 Отслеживание прихода ERC20 токенов на указанные кошельки через Moralis API
- 💰 Автоматическое определение стоимости токенов в USD
- 📱 Уведомления в Telegram о приходе токенов
- 📉 Автоматическое открытие шорт позиций на Gate.io при достижении пороговой суммы
- 🎛️ Управление через Telegram бота

## Установка

1. Клонируйте репозиторий и установите зависимости:

```bash
npm install
```

2. Создайте файл `.env` на основе `.env.example` и заполните необходимые переменные:

```bash
cp .env.example .env
```

3. Получите необходимые API ключи:

- **Telegram Bot Token**: Создайте бота через [@BotFather](https://t.me/botfather) в Telegram
- **Moralis API Key**: Получите на [Moralis Admin Panel](https://admin.moralis.io/)
- **Gate.io API Keys**: Создайте API ключи в настройках аккаунта на Gate.io

## Конфигурация

### Переменные окружения

- `TELEGRAM_BOT_TOKEN` - Токен Telegram бота
- `MORALIS_API_KEY` - API ключ Moralis (получите на https://admin.moralis.io/)
- `GATEIO_API_KEY` - API ключ Gate.io
- `GATEIO_SECRET_KEY` - Секретный ключ Gate.io

## Запуск

### Разработка

```bash
npm run start:dev
```

### Продакшн

```bash
npm run build
npm run start:prod
```

## Использование Telegram бота

После запуска приложения, найдите вашего бота в Telegram и отправьте команду `/start`.

### Доступные команды

- `/add_wallet <address>` - Добавить кошелек для отслеживания
- `/set_threshold <amount>` - Установить пороговую сумму в USD
- `/list_wallets` - Показать список отслеживаемых кошельков
- `/remove_wallet <address>` - Удалить кошелек из отслеживания
- `/transactions` - Показать последние транзакции
- `/help` - Показать справку по командам

### Пример использования

1. Добавьте кошелек:
```
/add_wallet 0x1234567890123456789012345678901234567890
```

2. Установите пороговую сумму:
```
/set_threshold 1000
```

3. Бот будет автоматически отслеживать приход токенов и уведомлять вас, а при достижении порога - открывать шорт позицию на Gate.io

## Архитектура

Проект состоит из следующих модулей:

- **DatabaseModule** - Работа с базой данных (SQLite)
- **TelegramModule** - Telegram бот и команды управления
- **BscTrackerModule** - Отслеживание транзакций через Moralis API
- **PriceModule** - Получение цен токенов в USD через CoinGecko
- **GateioModule** - Интеграция с Gate.io API для торговли

## База данных

Приложение использует SQLite базу данных (`bsc_tracker.db`), которая создается автоматически при первом запуске.

### Таблицы

- `wallets` - Отслеживаемые кошельки
- `transactions` - История транзакций

## Примечания

- Приложение использует Moralis Streams API для получения уведомлений о транзакциях в реальном времени через webhooks
- Webhook endpoint: `http://your-domain/webhook/moralis`
- Для работы webhooks необходимо, чтобы ваш сервер был доступен из интернета (используйте ngrok для разработки)
- Пороговая сумма по умолчанию: $1000 USD
- Шорт позиция открывается на сумму равную 1% от стоимости пришедших токенов
- Для работы Gate.io API требуется наличие средств на торговом аккаунте

## Деплой на Render

### Быстрый деплой через Render Dashboard:

1. **Подключите репозиторий** к Render через GitHub/GitLab
2. **Создайте новый Web Service**
3. **Настройки:**
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm run start:prod`
   - **Environment**: `Node`
4. **Добавьте переменные окружения:**
   - `TELEGRAM_BOT_TOKEN` - Токен Telegram бота
   - `MORALIS_API_KEY` - API ключ Moralis
   - `GATEIO_API_KEY` - API ключ Gate.io
   - `GATEIO_API_SECRET` - Секретный ключ Gate.io
   - `WEBHOOK_BASE_URL` - URL вашего Render сервиса (например: `https://your-app.onrender.com`)
   - `NODE_ENV` - `production`
5. **Деплой** - Render автоматически соберет и запустит приложение

### Использование render.yaml:

Если используете `render.yaml`, просто подключите репозиторий и Render автоматически применит конфигурацию.

**Важно:** После деплоя обновите `WEBHOOK_BASE_URL` в настройках Render на ваш реальный URL (например: `https://your-app.onrender.com`).

## Настройка Webhooks

Moralis требует публичный URL для отправки webhooks.

### Для разработки (локально):

Используйте Cloudflare Tunnel или ngrok:

```bash
cloudflared tunnel --url http://localhost:3001
```

Затем установите `WEBHOOK_BASE_URL` в `.env` на полученный URL.

### Для продакшена (Render):

После деплоя на Render, установите `WEBHOOK_BASE_URL` на URL вашего Render сервиса (например: `https://your-app.onrender.com`).

## Лицензия

ISC

# binance-alpha
